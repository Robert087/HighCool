# HighCool Remaining Work

This backlog is ordered for correctness, traceability, and safety. “Risk” describes the risk of leaving the task undone; implementation hazards are included in the description/dependencies where material.

Batch 1 verification update on 2026-07-25: the destructive SQLite startup behavior is addressed and locally verified by the Desktop Foundation Batch 1 build/test/runtime pass.

Batch 2 verification update on 2026-07-25: encrypted local backup manifests, mandatory pre-upgrade backup orchestration, upgrade/restore journals, restore preflight/execution service contracts, retention policy, desktop configuration profile, and startup diagnostics were implemented and backend-verified. Keep task 1 as a CI regression guard until the same checks run in CI and a desktop-host restore drill exists.

Batch 3 verification update on 2026-07-25: a separated Tauri desktop shell foundation, repeatable self-contained backend publish workflow, Desktop-profile loopback backend startup, token-protected diagnostics/shutdown endpoints, same-origin React static serving, and desktop build/tests were implemented. The Linux release build and non-interactive backend/static/API smoke passed, but a durable real-window desktop smoke must still pass on a normal desktop session before final approval.

Batch 3.1 verification update on 2026-07-25: observed WSLg readiness/lifecycle defects were fixed, including support-code parsing, transient readiness retry, app-exit backend cleanup, and Linux forced-termination backend cleanup. Targeted WSLg checks passed for startup-to-main transition, one loopback backend, auth/API route access, single-instance process behavior, normal-close cleanup, restart persistence, and forced-termination orphan cleanup. Keep the remaining full devtools/external-navigation/protected-shutdown/startup-failure matrix open.

Batch 3.2 verification update on 2026-07-25: automated backend/frontend/desktop regression checks passed, and the documented desktop Cargo commands now run from `src/desktop` through a Cargo workspace. Full approval remains blocked because only WSLg was available locally; the required normal Linux desktop and Windows verification matrices still need to be executed.

Pre-commit security hardening update on 2026-07-25: the unsafe committed JWT fallback secret and default SQL Server `sa` credential were removed; JWT startup now rejects missing/short/placeholder secrets outside Development/Testing; public password-reset and email-verification request endpoints now return generic token-free responses and deliver tokens only through an internal delivery abstraction. Keep the remaining invitation audit sanitization, real email/link delivery, frontend journeys, throttling, and broader perimeter work open.

Batch 5 backup/restore UI update on 2026-07-25: the authenticated `/settings/backup-restore` page, managed backup catalog/details/verify endpoints, Desktop-only local-database API gate, restore preflight operation IDs, restore replay/expiry/user/backup binding checks, operation serialization, and retention UI were implemented and tested. Keep scheduled backups, cloud/R2 backups, backup deletion, final installer/updater, Windows packaging, production Windows key provider, and normal-desktop interactive restore approval open.

## Critical fixes

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 1 | Keep SQLite startup and desktop data-safety behavior non-destructive | Batch 1 replaced the “tables without migration history = delete DB” heuristic with non-destructive schema checks, metadata, local path resolution, and explicit Development-only reset. Batch 2 added encrypted backup manifests, mandatory pre-upgrade backups, upgrade/restore journals, restore preflight/execution service contracts, retention, and startup diagnostics. Batch 3 added the Tauri shell foundation, self-contained backend publish workflow, Desktop-profile loopback startup, token-protected diagnostics/shutdown, and same-origin React static serving. Batch 3.1 fixed readiness parsing/retry, app-exit cleanup, and Linux forced-termination cleanup. Batch 3.2 kept automated checks green and fixed the documented `src/desktop` Cargo verification command path. Batch 5 added authenticated backup/restore UI, managed backup catalog/details/verify/retention flows, Desktop-only API gating, restore operation-token binding, and local backup/restore/retention operation serialization. Keep this as a CI and desktop-host regression guard. | `Infrastructure/DevelopmentDatabaseInitializer.cs`; `Infrastructure/LocalData/*`; `Application/LocalData/*`; `Domain/System/ApplicationDatabase*Journal.cs`; `Api/Endpoints/DesktopEndpoints.cs`; `Api/Endpoints/LocalDatabaseEndpoints.cs`; `src/frontend/src/pages/settings/SettingsBackupRestorePage.tsx`; `src/desktop/*`; initializer and desktop foundation tests | Local business data must never be erased or silently overwritten by normal startup, upgrade, backup, restore, or desktop process lifecycle flows. | CI verification; full normal-desktop failure/security matrix; production Windows key provider; scheduled/cloud backup policy; final installer/updater decisions. | Critical: local business data loss or orphaned local backend if this regresses. | Medium | CI starts the initializer twice and preserves a sentinel row; unsupported schema fails without deletion unless explicit Development reset is enabled; reset is impossible outside Development; encrypted backups restore through validation; upgrade requires a verified backup; restore requires preflight/confirmation/safety backup/operation token; restore tokens expire and cannot be replayed across backups or users; retention never deletes newest/active/invalid manifests; desktop shell starts exactly one loopback Desktop backend; React is served from the local backend; interactive desktop window, second launch, close shutdown, restart preservation, forced-termination cleanup, normal Linux/Windows security/failure matrices, and desktop-host restore drill pass on a normal desktop environment. |
| 2 | Keep auth secrets fail-closed and finish remaining token-exposure cleanup | Done on 2026-07-25: production/non-development startup rejects missing, short, or placeholder JWT secrets; Development/Testing can generate a local key outside the repo; Desktop reuses an app-data key and passes it by environment; public reset/verification request responses are generic and token-free; committed default SQL Server credentials were removed. Remaining: remove invitation tokens from audit payloads, sanitize historical audit rows where applicable, and keep regression tests/scans in CI. | `Infrastructure/Security/JwtSigningConfiguration.cs`; `JwtTokenService.cs`; `IdentityEndpoints.cs`; `IdentityService.cs`; `OrganizationAdministrationService.InviteUserAsync`; appsettings; `.gitignore`; identity tests | Resolved defaults prevent predictable JWT signing and public reset/verification token disclosure, but invitation token audit exposure can still leak account-onboarding access. | Email/link delivery design (task 9); deployment secret store (task 30); audit-data migration/sanitization decision. | Critical: invitation token leakage and regression could permit account takeover. | Medium | Production startup fails without valid auth config; public recovery/verification responses are generic and token-free; audit rows contain no usable token; scans find no committed fallback/default credentials; automated security tests prove it. |
| 3 | Make business uniqueness tenant-scoped | Change all organization-owned unique indexes to include `OrganizationId`, including master codes, UOM conversions, reason codes, and document numbers. | `Persistence/Configurations/*`; migrations; schema docs; service duplicate checks | Current indexes cause cross-tenant collisions and reveal business-key existence. | Data audit for existing duplicates; migration/rollback plan. | Critical: tenant isolation failure and onboarding blockers. | Large | Two organizations can create the same codes/numbers; duplicates within one organization return 409; migration applies to SQL Server from current baseline; cross-tenant tests pass. |
| 4 | Add concurrency-safe posting invariants | Protect remaining PO quantity, returnable receipt quantity, shortage open quantity, payment target open amount, running balances, and number generation using row versions/locks/serializable operations plus conflict handling. | Receipt/return/payment/shortage posting and validation services; ledger posting services; entities/configurations/endpoints | Ordinary transactions do not prevent two posts from validating the same open state. | Choose optimistic versus pessimistic strategy; SQL Server integration test environment. | Critical: over-receipt, over-return, over-settlement, corrupted running balances. | Extra Large | Parallel tests yield exactly one successful conflicting post; loser receives 409 with no partial effects; ledger/balance invariants reconcile after stress tests. |
| 5 | Define and enforce currency accounting | Choose single organization currency or full multi-currency. Validate all documents, allocations, statement rows, exchange rates, running balances, summaries, and UI defaults accordingly. | Payment/statement/shortage services/entities; `SupplierBalanceService.cs`; frontend finance forms/dashboard/formatters | Current statement rows can mix null, EGP, and user-selected currencies while balances sum them directly. | Product/accounting decision; migration policy for existing null currency rows. | Critical: materially wrong supplier balances. | Extra Large | Currency mismatch cannot enter a ledger; balances are either base-currency converted with traceability or partitioned by currency; forms use organization currency; reconciliation tests pass. |

## Security fixes

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 6 | Authorize and restore organization setup/feature gates | Require administrative permission for setup saves/completion; remove temporary bypasses; centralize module feature policy across UI, endpoints, and posting side effects. | `OrganizationSetupEndpointFilter.cs`; temporary flag files; organization admin service; `RouteGate.tsx`; `AppShell.tsx`; posting services | Any active member can mutate setup; disabled features are exposed and inconsistently honored. | Confirm which flags are MVP-supported and behavior after setup completion. | Critical: unauthorized configuration changes and inconsistent ledger effects. | Large | Non-admin setup mutation returns 403; incomplete setup routes correctly; disabled modules are hidden and API-blocked; posting effects follow one tested policy. |
| 7 | Enforce warehouse and branch scopes | Apply scoped predicates/guards to list, detail, create, update, post, reverse, dashboard, lookup, and statement/stock flows. Add branch keys only if branches are in current product scope. | `AuthorizationService.cs`; membership access entities; all warehouse-bearing services/endpoints; queries | Scope data is stored but operationally ignored. | Product decision on document visibility and branch domain model. | Critical: users access unauthorized warehouse data. | Extra Large | Restricted users cannot infer/read/write/post outside assigned scope; owners/all-scope retain access; tests cover list/detail/mutation and cross-scope IDs. |
| 8 | Implement real two-factor authentication or remove the flags | Add challenge state, factor enrollment/verification, limited pre-auth token, recovery, and audit; do not issue a full workspace JWT until completed. | Identity DTOs/endpoints/service; security settings; auth provider/pages | `ForceTwoFactor`/OTP flags currently signal a requirement but grant full access anyway. | Email/SMS/TOTP provider and recovery policy. | High: false security control. | Extra Large | Enabling 2FA blocks protected API access until verified; factor enrollment/recovery is auditable; disabling behaves predictably; E2E tests cover both paths. |
| 9 | Complete secure email invitation/recovery/verification delivery | Integrate an email provider and app base URL, send one-time links through the existing auth-message delivery seam, add frontend accept/forgot/reset/verify routes, preserve generic anti-enumeration responses, add expiry/revocation, and resend controls. | Identity service/endpoints/token tables; invitations; auth pages/routes/services | Reset/verification raw-token API exposure is fixed, but production users still cannot receive links because the default delivery implementation is no-op and invitation delivery remains incomplete. | Task 2; provider/product choice; email templates/localization. | High: users cannot safely onboard or recover accounts. | Large | No raw token is returned/logged; links work once and expire; unknown-email response is indistinguishable; localized E2E tests pass. |
| 10 | Harden API/session/browser perimeter | Add rate limits, HTTPS/HSTS expectations, forwarded headers, CSP/security headers, safe CORS config, central 401 logout/redirect, and optional HttpOnly cookie evaluation. | `Program.cs`; auth middleware; `api.ts`; `AuthProvider.tsx`; deployment proxy | Brute force, XSS token exposure, and stale expired-session UI remain. | Hosting topology; task 30. | High: credential abuse/session compromise. | Large | Rate-limit tests pass; production headers score against agreed baseline; CORS is environment-configured; 401 clears context and redirects; security review documents token storage choice. |

## Data-integrity fixes

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 11 | Populate document lifecycle audit metadata | Set `PostedAt/PostedBy`, `CanceledAt/CanceledBy`, and consistent reversal metadata atomically; expose where useful. | `BusinessDocument.cs`; every posting/cancel/reversal service; DTOs/tests | Current dedicated lifecycle fields remain null, weakening auditability. | Actor/time convention. | High: incomplete audit trail. | Medium | All lifecycle transitions set immutable actor/time fields exactly once; repost is idempotent; tests assert values. |
| 12 | Implement organization-scoped document numbering | Replace hardcoded/timestamp prefixes with transactional, organization/document/fiscal-year sequences using configured prefixes. | Document services; `Organization.cs`; migrations/configuration; setup/settings UI | Configured prefixes are ignored and timestamp numbering can collide. | Task 3 and task 4; numbering format decision. | Medium: duplicate/incorrect legal document identifiers. | Large | Numbers use active organization prefix/sequence, remain unique under concurrency, never renumber posted docs, and have migration-safe configuration. |
| 13 | Complete purchase-return correction flow | Define and implement reversal or controlled cancellation for posted returns with opposite stock/statement effects and dependency checks. | Purchase return domain/services/endpoints/pages; reversal dispatch/services; posting matrix | Posted return errors have no compliant correction path. | Product decision; currency and concurrency fixes. | High: permanent incorrect inventory/payables. | Large | Posted return cannot be edited/deleted; authorized reversal creates traceable opposite effects; duplicate/downstream-invalid reversal is blocked; tests reconcile ledgers. |
| 14 | Make feature flags enforce business effects consistently | Audit every organization flag; implement behavior or remove/hide unsupported flags such as approval, negative stock, auto-post, reversals, stock tracking, transfers, and adjustments. | `Organization.cs`; setup DTO/page; posting services; feature config | Configuration currently overpromises and some flags affect only one path. | Task 6; product scope decisions. | High: configuration says one thing while ledgers do another. | Large | Each exposed flag has documented server behavior and tests, or is absent from API/UI; no client-only enforcement. |
| 15 | Correct tenant-unaware frontend caches | Key master-data options and dashboard snapshots by organization; invalidate on switch/logout and relevant mutations; cancel stale in-flight writes. | `masterDataApi.ts`; `dashboardApi.ts`; `AuthProvider.tsx`; feature provider | Warm caches can display data from the previously selected organization. | Stable organization-switch event/API. | High: cross-tenant data disclosure in UI. | Medium | Switching organizations never shows prior-org cached data; tests cover warm cache and in-flight response races; mutation invalidation remains correct. |

## Incomplete core features

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 16 | Reconcile and complete settings UX | Select one settings architecture; route organization, security, profiles, invitations, sessions, audit, and features; expose supported create/revoke/clone/ownership/access-scope actions. | `AppRoutes.tsx`; `OrganizationSettingsPage.tsx`; `pages/settings/*`; `settingsApi.ts` | Most backend settings features are unreachable and duplicate page implementations exist. | Tasks 6–10; product IA decision. | Medium: admins cannot operate security/configuration safely. | Large | Every routed settings page has permission gate, loading/empty/error/success states, localized forms, functional mutations, and integration tests; duplicates removed. |
| 17 | Add shortage reason-code administration | Add paginated CRUD/deactivation API/UI with validation and usage protection. | Reason-code domain/service/endpoint; new page/form; migrations if fields change | Users can only consume seeded/existing reasons and cannot administer them. | Task 25 list standard. | Medium: operational setup cannot be maintained. | Medium | Admin can create/edit/deactivate; receipt selector shows active values; historical rows retain references; permission/tests/docs added. |
| 18 | Implement IndexedDB offline drafts | Add versioned local draft schema for approved document forms, explicit sync/reopen/discard, organization/user scoping, pending count, encryption/privacy decision, and never post offline. | New offline feature; form hooks; `AppShell.tsx`; auth switch/logout | “Offline drafts only” is promised but absent. | Product conflict/retention policy; stable form DTO versions. | Medium: draft loss and misleading offline UI. | Extra Large | Drafts survive reload/offline, are isolated by org/user, cannot post offline, show pending count, migrate safely, and reconcile with server edit conflicts. |
| 19 | Deliver sales and customer financial core | Define sales order/invoice/return, stock OUT, customer statement, collection, allocation, and reversal flows following current ledger rules. | New domain/application/infrastructure/API/frontend/docs/migrations/tests | Required by the project mission; current customer module is master data only. | Stabilized platform through Phase 2; product posting/valuation/tax decisions. | High: full ERP mission remains incomplete. | Extra Large | Approved posting matrix; paginated APIs; localized UI; idempotent posting/reversal; stock/customer-ledger reconciliation; integration/E2E tests. |
| 20 | Deliver supplier commissions and workforce modules | After sales stabilization, define supplier commission events/settlements and employees/advances/payroll with allocation, audit, and reversal rules. | New modules across all layers | Required mission areas have no implementation. | Product specifications; accounting/payroll/legal rules; task 19/platform stabilization. | Medium: scope incompleteness; high implementation risk if rushed. | Extra Large | Signed domain/posting/schema/API specs; append-only financial effects; permissions; localized UI; tests; deployment/migration docs. |
| 21 | Implement approved inventory operations | Build only approved stock adjustments, transfers, counts/opening balances, and batch/serial/expiry workflows; enforce negative-stock policy. | Inventory domain/services/endpoints/pages; organization flags | Flags/permissions exist without workflows. | Product scope and costing/traceability decisions; task 14. | High: operators may expect unsupported inventory controls. | Extra Large | Each enabled operation posts ledger rows only, is idempotent/reversible, paginated, permission/scoped, localized, and tested. |

## UX improvements

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 22 | Finish Arabic/English localization | Replace all visible literals with shared keys; localize validation/toasts/filters/statuses/buttons; verify mixed-direction codes/numbers. | Major pages/forms; shared components; `messages.ts`; formatters | Current Arabic mode retains extensive English content. | Canonical page/components from task 26. | High: violates first-class Arabic requirement. | Large | Automated literal/key parity checks pass; every shipped route is manually/E2E verified in English LTR and Arabic RTL; locale-aware values used everywhere. |
| 23 | Make actions permission-aware | Add all permission constants to frontend; gate create/edit/post/cancel/reverse/deactivate actions and route form mode appropriately. | `services/permissions.ts`; routes; AppShell; page actions/forms | View-only users see forbidden actions and receive avoidable 403 errors. | Stable permission matrix; task 6. | Medium: confusing UX and accidental forbidden operations. | Medium | Each action is hidden/disabled with correct rationale; direct navigation is safely gated; backend remains authoritative; role-matrix E2E tests pass. |
| 24 | Replace dashboard fan-out with accurate summaries | Add dedicated server read model for all KPIs/queues; remove first-row sums and capped scans; key cache by organization. | New dashboard query endpoint/service; `dashboardApi.ts`; `DashboardPage.tsx` | Current financial, shortage, and negative-stock KPIs are wrong/incomplete. | Currency rule; task 15; agreed KPI definitions. | High: management decisions based on false metrics. | Large | Every KPI reconciles to direct database queries on multi-row data; one/few bounded requests; authorization/scoping tests; freshness policy documented. |
| 25 | Make shell promises real or remove them | Connect global search/notifications/pending drafts/AI to real services, or relabel/remove placeholders. | `AppShell.tsx`; notification/search/offline modules | Static notifications and navigation-only search look production-functional. | Product priorities; task 18 for drafts. | Medium: misleading enterprise UX. | Large | No visible placeholder claims; search coverage matches label; notifications persist/read; pending count is real; inaccessible results are filtered. |
| 26 | Accessibility and responsive workflow audit | Test keyboard/focus/dialog/menu/table/form/error semantics, contrast, screen readers, mobile layouts, and RTL. | Shared UI, CSS, all major forms/lists | Shared components are thoughtful but no automated/browser accessibility evidence exists. | Localization completion and E2E tooling. | Medium: inaccessible or unusable workflows. | Large | Agreed WCAG baseline passes automated/manual audit; focus restoration and keyboard navigation work; core mobile/RTL flows pass. |

## Refactoring tasks

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 27 | Centralize API validation and error contracts | Add ProblemDetails exception middleware, validation filter, 409 concurrency/duplicate mapping, correlation ID, and frontend parser contract. | `Program.cs`; all endpoint files; validators; `api.ts` | Repeated helpers and unhandled DB exceptions produce inconsistent errors. | Concurrency exception design. | Medium: opaque production failures and duplicate code. | Medium | All endpoints use documented status/problem shape; validation field keys are stable; unhandled exceptions are logged with correlation ID and sanitized to clients. |
| 28 | Standardize paginated list and lookup APIs | Convert master data and audit to bounded `PagedResult`; add lightweight active-option endpoints/caching; cap every page size. | Master-data services/endpoints/DTOs/pages; audit; option loaders | Current unbounded lists violate architecture/performance rules. | Index review; API version/compatibility plan. | High: performance degradation and browser memory/network cost. | Large | No operational endpoint returns unbounded rows; server filters/sorts/pages; option endpoints are bounded/lightweight; pagination tests cover edges. |
| 29 | Split oversized services and pages | Decompose organization admin, identity, receipt/shortage services and large forms into cohesive commands/queries/hooks/components without changing behavior. | Largest backend services and frontend pages listed in audit | Files are hard to reason about and change safely. | Characterization tests and stable contracts. | Medium: regression risk and slow feature delivery. | Extra Large | Responsibilities have clear boundaries; no material behavior diff; tests remain green; dependency directions stay layered; agreed size/complexity thresholds met. |
| 30 | Establish generated/verified API contracts and cache policy | Publish OpenAPI, generate or contract-test TS DTOs, standardize query keys/cancellation/invalidation, and eliminate ad hoc retry behavior. | API explorer/config; frontend services; caches; Vite proxy | Handwritten C#/TS types and custom caches can drift or cross tenant boundaries. | Task 27/28; tooling decision. | Medium: runtime mapping bugs and stale data. | Large | CI detects contract drift; requests support abort; cache keys include organization/parameters; retries are method-safe; mutations invalidate documented queries. |

## Testing tasks

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 31 | Create a reproducible CI quality gate | Pin SDK/runtime, use `npm ci`/restore, add lint/format/typecheck/build/unit/integration jobs and coverage/artifacts. | New CI workflow; package scripts/config; solution; lockfiles | Audit environment could not build because dependencies/SDK were absent; repository has no CI/lint. | CI provider; runtime version policy. | High: broken code can merge unnoticed. | Medium | Clean runner restores and passes backend build/test, frontend lint/typecheck/test/build; caches are safe; failures block merge; versions are documented. |
| 32 | Add critical SQL Server, tenant, concurrency, and migration tests | Run real SQL Server integration tests for current migrations, tenant indexes/scopes, parallel posts, rollback, and migration from baseline. | Backend test projects; container/test DB setup; migrations | SQLite/InMemory cannot prove SQL Server locking/index/migration behavior. | Tasks 3–7; CI service/container. | Critical: provider-specific production corruption/migration failure. | Extra Large | Tests reproduce old races/collisions and prove fixes; current baseline upgrades; rollback/failed post leaves no effects; suite runs in CI. |
| 33 | Add browser E2E, RTL, accessibility, and offline tests | Cover login/setup, each document lifecycle, allocations/reversals, settings, org switch, permissions, Arabic, mobile, session expiry, and offline drafts. | New E2E project; frontend/backend fixtures | Frontend has only 11 low-level tests and no real browser workflows. | Stable phases 1–2; deterministic seed factory. | High: integration and UX regressions. | Extra Large | Critical journey suite passes in English/Arabic and desktop/mobile; accessibility assertions run; no shared-state flakiness; screenshots/traces retained on failure. |

## Deployment tasks

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 34 | Build production packaging and deployment pipeline | Define static frontend/API topology, Docker or host artifacts, same-origin proxy/CORS, secret injection, migration job, staging promotion, rollback, and health/readiness. | New deployment files; `Program.cs`; Vite config; CI/CD docs | No production host, container, pipeline, or migration orchestration exists. | Phase 0 security; target platform; task 31. | Critical: unsafe/manual releases and outages. | Extra Large | One command/pipeline deploys staging from clean commit; secrets never enter images/logs; DB migration is gated; readiness includes DB; rollback is tested. |
| 35 | Add observability, backups, and operational runbooks | Structured logs/correlation, metrics/traces, business posting alerts, error tracking, DB backup retention, restore drills, audit retention, incident/recovery runbooks. | API/infrastructure; hosting; documentation | Health only says `OK`; no monitoring/backup evidence exists. | Hosting/SQL platform; RPO/RTO and retention requirements. | Critical: silent corruption/outage and unrecoverable data. | Large | Dashboards/alerts cover errors, latency, posting conflicts, DB health; backup restore meets RPO/RTO; sensitive fields are redacted; runbooks are exercised. |

## Documentation tasks

| Order | Task title | Description | Related files | Reason needed | Dependencies | Risk | Complexity | Acceptance criteria |
|---:|---|---|---|---|---|---|---|---|
| 36 | Reconcile architecture, API, schema, and setup documentation | Make `docs/` authoritative; remove/redirect divergent root duplicates; update README, API, identity/settings, schema/ERD, environment example, posting matrix, and current scope. | `README.md`; root duplicate docs; all `docs/*.md`; new safe `.env.example` | Current docs contradict code and include obsolete paths. | Product answers and completed Phase 0 changes. | Medium: developers make unsafe changes from stale guidance. | Medium | One authoritative document set has owner/date/version; every endpoint/table/module is represented; setup works from clean machine; no secret values or stale absolute paths. |

## Recommended implementation order summary

1. Stabilize data/auth/tenant/currency/concurrency (1–5).
2. Restore real security and access enforcement (6–10).
3. Close lifecycle and feature-integrity gaps (11–15).
4. Complete the current operational product slice (16–18).
5. Standardize UX/contracts/refactoring and quality (22–33).
6. Establish production operations (34–36).
7. Only then expand major business scope (19–21), sequenced by approved product priorities.
